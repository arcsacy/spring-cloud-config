server:
  port: 9003
logging:
  file:
    name: /logs/${spring.application.name}.log
  level:
    com.netflex: warn
    com.tyr: debug
swagger:
  lib:
    enabled: true
    title: 业务服务
    description: 业务服务接口文档
    version: 1.0
    base-package: com.tyr.business.controller
feign:
  hystrix:
    enabled: true